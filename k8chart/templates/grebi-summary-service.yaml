apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-summary-service
  labels:
    app: {{ .Release.Name }}-summary-service
spec:
  ports:
      - port: 8080
        targetPort: 8080
        name: http
        protocol: TCP
  selector:
    app: {{ .Release.Name }}-summary-service