
import { Grid } from "@mui/material";
import GraphNode from "../../../model/GraphNode";
import React, { Fragment } from "react";

export default function EdgesOutList(params:{
    subgraph:string,
    node:GraphNode,
    datasources:string[],
    dsEnabled:string[]
}) {
    let { subgraph, node, datasources, dsEnabled } = params
    
    

    return <div/>

}
